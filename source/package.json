{
  "name": "flappy",
  "version": "1.0.0",
  "description": "",
  "main": "app.ts",
  "dependencies": {
    "express": "^4.17.1",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "@types/express": "^4.17.11",
    "concurrently": "^5.2.0",
    "nodemon": "^2.0.7",
    "typescript": "^4.1.5"
  },
  "scripts": {
    "tsc:watch": "tsc --watch",
    "nodemon:watch": "nodemon --config nodemon.json",
    "start:dev": "concurrently -n \"tsc,node\" --prefix-colors \"gray,green\" \"tsc --watch\" \"nodemon --config nodemon.json\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
